{
    "tasks": [
        {
            "type": "cargo",
            "label": "lldb cargo",
            "command": "build",
            // "args": [
            //     "build"
            // ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}